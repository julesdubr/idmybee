"""One-off, ad hoc scripts -- not part of the regular pipeline, run by hand
when a specific need comes up (a TPS needs cleaning up, a folder of images
needs its EXIF orientation fixed, a legacy naming scheme needs flattening).
None of these are imported by another module; each is launched standalone
with `python -m tools.maintenance.<script>`.
"""
